runtime: python312

entrypoint: python manage.py migrate --noinput && gunicorn -b :$PORT b3rc_site.wsgi

instance_class: F1
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 2

env_variables:
  DJANGO_SETTINGS_MODULE: "b3rc_site.settings"
  DJANGO_SECRET_KEY: "django-insecure-dxhy6pjxrc3j$icrk-%1^-p7-3fkz9-9h+^5_&q$$0u@!4s1-_"
  DB_PASSWORD: "b3rc2026db"

handlers:
  - url: /static
    static_dir: staticfiles

  - url: /.*
    script: auto

